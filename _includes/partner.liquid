<div class="col">
  <a href="{% if partner.redirect %}{{ partner.redirect }}{% else %}{{ partner.url | relative_url }}{% endif %}">
    <!-- div class="card h-100 hoverable" -->
    {% if partner.logo %}
      {% include figure.liquid
 loading="eager"
 path=partner.logo
 sizes = "250px"
 alt=partner.label
 title=partner.label
 class="card-img-top"
      %}
    {% endif %}
  <!-- div class="card-body">
    <h2 class="card-title">{{ partner.label }}</h2>
  <p class="card-text">{{ partner.short_description }}</p>
    <div class="row ml-1 mr-1 p-0">
      {% if partner.details %}
        <p>{{ partner.details  }}</p>
      {% endif %}
    </div>
  </div -->
  <!-- /div -->
  </a>
</div>